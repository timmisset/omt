model:
    MijnActiviteit: !Activity
        title: Mijn Activiteit

        variables:
        -   $variableA
        -   $variableB

        watchers:
            -   query: $variable / GRAPH
                onChange: |
                    @LOG($newValue);

